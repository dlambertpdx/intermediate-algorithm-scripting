
function sumFibs(num) {
  var a = 0;
  var b = 1;
  var result = 0;
  
  while(b <= num){
    if(b % 2 !== 0){
      result += b;
    }
    b += a; 
    a = b - a;
  }
  return result;
}

sumFibs(1000);
